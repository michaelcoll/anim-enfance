language: java
sudo: required
services:
  - docker
node_js:
  - "stable"
notifications:
  slack: animenfance:5XpEfsL90X6C7qGqEAJ76ZRS
cache:
  directories:
    - $HOME/.m2
script:
  - mvn package
after_success:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push michaelcoll/anim-enfance-back
